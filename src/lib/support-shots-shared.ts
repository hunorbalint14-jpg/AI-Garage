// Client-safe constants for support-widget screenshots. The server-only
// storage helpers live in ./support-shots (supabase/admin import).
export const SUPPORT_SHOT_BUCKET = "support-shots";
export const SUPPORT_SHOT_MAX_BYTES = 4 * 1024 * 1024; // 4 MB
